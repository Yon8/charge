<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lxy.charge.mapper.system.SysUserMapper">
    <resultMap id="BaseResultMap" type="com.lxy.charge.pojo.system.SysUser">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="role_id" property="roleId" jdbcType="INTEGER"/>
        <result column="gender" property="gender" jdbcType="INTEGER"/>
        <result column="birthday" property="birthday" jdbcType="DATE"/>
        <result column="address" property="address" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="del_flag" property="delFlag" jdbcType="INTEGER"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="remarks" property="remarks" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="getSysUserList" resultMap="BaseResultMap">
        select u.*,r.name as role from sys_user u LEFT JOIN sys_role r on u.role_id = r.id
        <where>
            <if test="id != null" >
                and id = #{id,jdbcType=INTEGER}
            </if>
            <if test="name != null" >
                and name like concat('%', #{name,jdbcType=VARCHAR}, '%')
            </if>
        </where>
    </select>
    <select id="findOneByUserName" parameterType="String" resultMap="BaseResultMap">
        select * from sys_user where username=#{username, jdbcType=VARCHAR}
    </select>
    <select id="getRoleByUserId" resultType="com.lxy.charge.pojo.system.SysRole" parameterType="java.lang.Integer" >
        select r.name from sys_user u LEFT JOIN sys_role r on u.role_id = r.id WHERE u.id = #{id,jdbcType=INTEGER}
    </select>
    <select id="getUserIdAndName" resultType="com.lxy.charge.pojo.system.SysUser" >
        select
            id,name
        from sys_user
    </select>
    <delete id="sysUserDelete">
        delete from sys_user where id=#{id}
    </delete>
    <insert id="sysUserAdd" parameterType="com.lxy.charge.pojo.system.SysUser">
        INSERT INTO sys_user (id, username, password, name, role_id, gender, birthday, address, phone, email, del_flag, update_time, create_time, remarks)
        VALUES (#{id, jdbcType=INTEGER}, #{username, jdbcType=VARCHAR}, #{password, jdbcType=VARCHAR}, #{name, jdbcType=VARCHAR},
                #{roleId, jdbcType=INTEGER}, #{gender, jdbcType=INTEGER}, #{birthday, jdbcType=DATE}, #{address, jdbcType=VARCHAR},
                #{phone, jdbcType=VARCHAR}, #{email, jdbcType=VARCHAR}, #{delFlag, jdbcType=INTEGER},
                #{updateTime, jdbcType=TIMESTAMP}, #{createTime, jdbcType=TIMESTAMP}, #{remarks, jdbcType=VARCHAR})
    </insert>

    <update id="sysUserEdit" parameterType="com.lxy.charge.pojo.system.SysUser">
        UPDATE sys_user SET
                            username = #{username, jdbcType=VARCHAR},
                            password = #{password, jdbcType=VARCHAR},
                            name = #{name, jdbcType=VARCHAR},
                            role_id = #{roleId, jdbcType=INTEGER},
                            gender = #{gender, jdbcType=INTEGER},
                            birthday = #{birthday, jdbcType=DATE},
                            address = #{address, jdbcType=VARCHAR},
                            phone = #{phone, jdbcType=VARCHAR},
                            email = #{email, jdbcType=VARCHAR},
                            del_flag = #{delFlag, jdbcType=INTEGER},
                            update_time = #{updateTime, jdbcType=TIMESTAMP},
                            create_time = #{createTime, jdbcType=TIMESTAMP},
                            remarks = #{remarks, jdbcType=VARCHAR}
        WHERE id = #{id, jdbcType=INTEGER}
    </update>

    <delete id="sysUserDeleteByIds">
        delete from sys_user where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id, jdbcType=INTEGER}
        </foreach>
    </delete>

</mapper>